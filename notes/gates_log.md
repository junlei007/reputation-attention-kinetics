# 门禁日志（gates log）

> 研究计划第 9 节定义的工作包门禁。每阶段结束记录判定与依据，向用户报告后再进入下一阶段。

---

## G1（WP0 + WP1 完成，2026-08-11）

**判定：通过 ✅** —— 继续资本反馈主路线（不触发降级）。

**依据**：
- 数据审计复现 README 全部数字（`experiments/audit.json`，含 35,592 行 / 5,881 用户 / 32,029 正 / 3,563 负 / 无自环 / 无重复有向对）。
- 8 张诊断图 + `experiments/facts.json`（74s 可复现运行）。
- **G1 探针**（conditional logit，12,000 事件 × 30 对照 + 负控制）：
  - count_cap +0.033 (p=1e-30)，net_cap −0.064 (p=2e-11)，log_recency −1.21，负控制系数 |β|<0.04；
  - ⇒ 资本反馈在控制活跃、年龄、入度后**显著可识别**，且符号依赖（net 条件效应为负）。

**关键冻结决策**（详见 `notes/WP0_model_freeze.md`）：
1. 主状态 = 一维衰减净值资本（hl=180d 基线）；count/quality 仅作稳健性变体；
2. Ψ 有界、Lipschitz、**符号自由**（不预设正反馈）；
3. 人口进入外生风险集，无出生项；
4. 一次性排除项占比仅 0.10% ⇒ 主定理可对无排除潜在系统陈述，排除项作显式修正；
5. 时间切分：主切分按时间跨度 70/30（cutoff 2014-07-03，测试 2,601 事件落在平台衰退尾部）；WP5 补充事件分位数切分 + expanding windows；
6. 标记 Q 必须带时间项（负评占比 2%→16%→5% 非平稳）。

**下一步**：阶段 2（WP2）——微观模拟器与合成参数恢复。

---

## G2（WP2 完成，2026-08-11）

**判定：通过 ✅** —— 核心参数在合成数据上可恢复，稀疏事件率复现。

**依据**（`experiments/synthetic_recovery.json`，双设计）：
- **主设计**（N=500, T=4, α2=0.8 强目标侧效应）：
  - 活动性 a_i：log-corr 0.93（全局尺度吸收进 W）✅
  - 核 W：log-corr 0.98，rel_rmse 0.086（logW 对照系数 0.94≈1 同步回收）✅
  - 资本函数 ψ2：条件 logit 探针系数 +1.33（se 0.22, p=3e-9），符号与形状正确 ✅
  - 标记 Q：b_target 0.94（真值 1.0）；b_sender 弱效应下噪声较大；幅度均匀 ✅
- **长时程设计**（N=200, T=90 天，覆盖 3–6 个半衰期）：
  - δ：**粗尺度恢复**——hl=15 与 hl≥30 清晰分离，真值 30 落在识别带 [30, 60] 内；30 vs 60 仍在 MC 噪声内（观测单次实现噪声 ~0.7%）
  - 活动性：corr 0.95
- **稀疏性**：事件/人 ≈ 4.4 恒定于 N ∈ {250, 500, 1000}（总事件 O(N)），复现稀疏网络性质 ✅
- **附带产出**：恢复实验抓出并修复模拟器真实 bug（一次性排除检查使用了过期的 used 集合，导致 1 对重复）——修复后 2042/2042 无重复对。

**识别结构发现（写入论文方法/局限）**：
1. 事件级似然（选择/标记）对 δ **几乎平坦**——线性响应模型对资本特征尺度不变，非线性曲率是唯一事件级通道；
2. δ 的识别通道是**聚合速率轨迹**（Λ(t) 的水平由 δ 通过资本积累控制），要求样本跨多个半衰期；
3. 因此 WP5 的 δ 选择必须用宏观层验证（测试期资本分布预测 / 轨迹 IAE，计划 §8.4-5 与 §8.6），并做半衰期敏感性分析——与研究计划对 δ 的定位一致，不做点估计声称。

**下一步**：阶段 3（WP3）——宏观方程推导与数值求解（微观—宏观对照，门禁 G3）。

---

## G3（WP3 完成，2026-08-11）

**判定：通过 ✅** —— 宏观解在合理网格下收敛到 MC，效率优势成立。

**依据**（`experiments/micro_macro.json`，块结构验证设计：确定性组活跃度、目标侧标记、全进入）：
- **单轮 W1(μ_N^T, f_T)**：0.0130 (N=250) → 0.0040 (N=4000)，拟合 **W1 ≈ 0.198·N^{-1/2}**，地板 ≈ 0 —— 传播混沌涨落定律在数值上精确成立，求解器网格精度低于涨落；
- **池化 W1（O(1/N) 偏置）**：0.0069 → 0.0024 单调下降；
- **人均事件率轨迹**：MC vs 宏观相对差 1.9%（δ 识别通道验证可用）；
- **效率**：宏观 0.48s（与 N 无关）vs MC 2.26s/轮（N=4000）；MC 成本 ~O(N²T) 随 N 二次增长，宏观恒定。

**本轮验证抓出并修复的 bug（写入论文复现说明的教训）**：
1. 模拟器：一次性排除检查用过期的 used 集合（G2 期发现）；
2. 求解器：q_cells 列序与 r_values 错位（负标记在前约定）；
3. 求解器：`_rate` 转置错误（目标率应累加发送方活跃度 a_l，曾误用目标方 a_k）；
4. 求解器：半拉格朗日 remap 中点单点求值产生每步 −h/2 均值漂移 → 保守 remap 修复；
5. 脚本：build_mc_params 的 activity 在 groups 覆盖前生成 → 组-活跃度错位（轨迹差 12% → 修复后 1.9%）；
6. 比较约定：cap_after（事件加权）≠ 人群分布（sim.C）——W1 对照必须用终态人群资本。

**交付物**：`kinetic.py`（有限体积求解器）、`notes/WP3_kinetic_derivation.md`（弱形式推导、
适定性条件 A1–A4、排除项 ε_history 出处、数值方案）、`scripts/04_micro_macro.py`。

**下一步**：阶段 4（WP4）——有限时间理论：适定性 → 传播混沌 → 定量误差 + ε_history。

---

## G4（WP4 草稿完成，2026-08-11；2026-08-11 复核修订）

**判定：条件通过 ✅/⏳（A1/JCN 理论优先路线）** —— 主定理（三项分解）成立，
但证明为 draft 状态：`paper/appendix/proofs.tex` 三处 proof sketch（T1/T2/T3）标有
"to be completed"，7 处 [V] 待逐项核验；正式处理为有限类型空间，连续 Graphon 情形
仅援引 BCW23 框架。**暂不足以直接投 EJAM**；补齐连续 Graphon 或路径空间一致收敛
或更强历史误差定理后再复核 EJAM 路由。

> sup_{t≤T} E W1(μ_t^N, f_t) ≤ C_T { N^{−1/2} + ‖W_N − W‖_∞ + N^{−1} }

含三项分解：粒子涨落（传播混沌，N^{-1/2}）、经验核距离、一次性排除项
**ε_history(N,T) ≤ C_T·N^{−1}**（比主项低一阶的显式有限 N 误差项）。

**交付物**：
- `notes/WP4_proof_sketch.md`——T1 适定性（Banach 不动点 + Grönwall 压缩）、T2 定量
  传播混沌（同步耦合 + 经验发送场鞅涨落 + 闭环不等式）、T3 误差分解、诚实清单
  （5 处 ⚠️ 待核验 + 假设松弛边界 + 已知边界表）；
- `paper/appendix/proofs.tex`——LaTeX 版（已编译通过，3 页 PDF），可成长为论文附录；
- **数值交叉验证**（G3 已独立确认涨落阶）：实测 W1 ≈ 0.198·N^{-1/2}，与定理的
  N^{-1/2} 阶一致——理论常数与模拟量级相容。

**路由判定（计划 WP6 第二条）**：*Journal of Complex Networks*——标准但严谨的
有限时间定理 + 强机制实证闭环（WP5 待做）。EJAM 升级路径已记录（显式常数追踪、
耗散统一时间、连续类型 Graphon 化——三者任一，用户有数学余量时）。

**⚠️ 数学核验责任**：证明草稿的正确性由用户核验（计划默认决策 #3）；
5 处 [V] 标记为关键估计，已在两版文档中列明。

## G4 补记（2026-08-11 二审修订）：理论重写 v0.3

**触发**：投稿前审计发现 proofs.tex v0.1/v0.2 存在**结构性缺口**（非仅常数
待精算）：(1) 类型固定后副本独立但非同分布，π 未进弱方程；(2) Q 依赖双方
资本时"共用标记"不成立；(3) 适定性证明对非概率测度使用 W1，压缩不成立；
(4) B_i 在微观有、极限方程与定理无，RfA 窗口不是当前定理的直接实例；
(5) N^{-1/2} 经验 W1 缺类型抽样与矩条件。

**v0.3 修订内容**（WP3 v0.2 + WP4 v0.3 + proofs.tex v0.3，均编译通过）：
1. 弱方程补类型权重 π_k；引入未归一化标记率核 M_{k,t}（消除 A_t=0 零分母）；
2. 传播混沌改用**支配泊松随机测度 + thinning + 最优标记耦合**，标记差经
   Q 的 W1-Lipschitz (A3) 进入闭环（η(1+L_Q) 项）；
3. 适定性改概率表示 + 路径耦合（W1 只作用于概率测度）；
4. 主定理明确假设 B_i≡1；RfA 窗口为条件实证风险集，**不是主定理实例**
   （叙述边界定稿；无排除情形对应 RfA 允许重复对）；
5. 初始条件加强为 2+ε 阶矩或紧支撑 (A5)；经验 W1 用完整一维分布函数引理
   （∫F(1−F)dx = E|X−X'|/2），不再引用式引用；
6. 误差结构升级为五元：C_T { ε_{0,N} + ‖π_N−π‖_1 + ‖W_N−W‖_∞ + N^{-1/2}
   + ε_history }，iid 类型下恢复三项简洁形式；
7. **ε_history 显式常数**：ε_history(N,T) ≤ C T² e^{LT} / N（T² = 已用对
   累积事件历史，e^{LT} = Grönwall 放大）——"事件历史"的显式化。

**v0.3 后状态**：结构性缺口全部消除，[V] 项（V1–V7）均为**常数精算**或
"结构已定、逐项写出"，不再有"论证本身可能不成立"的条目。剩 7 项作者核验
（见 WP4 §6 清单）。新意对照（审计要求）：对照 BCW23 与 AG25（Stochastic
Systems 15(4):273–290）——增量在"带符号标记资本反馈 + 一次性历史排除"，
排除项不属于 AG25 的无记忆扰动框架；机制层面无新于 BCW23，写法已诚实化。

## G4 补记（2026-08-11 三审核验）：v0.3 → v0.4 按核验意见修订

**用户核验结论（v0.3）**：T3 基本成立（补 A(0)=0、η 直接代价、i≠j 后通过）；
**T1 未通过**（P₂ 在 W1 下不完备；路径耦合又用归一化 Q̄，A_t→0 时 Lipschitz 不
成立；decay term 不属系数差；"same initial law, independent draws"应为同一实现）；
**T2 未通过**（Q̄ vs 单发送者分量的平均标记不等式存在反例：两类发送者各半、
一类恒 +1 一类恒 −1、L_Q=0 时 W1(Q̄,δ_{+1})=1 而右端 0；副本事件率误依赖其他
Ỹ_i；经验 W1 引理的无界区间 Cauchy–Schwarz 不成立）。

**v0.4 修订**（WP3/WP4/proofs.tex 同步，编译通过 6 页）：
1. **固定点空间改 P₁**（完备；P₂ 不完备已弃），矩传播单独证明；
2. **全程有限标记率测度 ν**（微观 ν_{j,t}^N，极限 ν_{k,t}[f] = ψ2·M），定义
   **D-度量**（带损失耦合：未匹配质量付 1、匹配标记差付 |r−r'|），从不归一化，
   A_t=0 处 ν 是零测度；
3. **标记耦合改"经验混合 vs 极限混合"**按类型匹配分量（反例已理解：混合 vs
   单分量不可比，混合 vs 混合经 A3 + W1 凸性可控）；副本率 = κ_t（极限场），
   同步耦合不改变边际；
4. (L_f − L_g) 无衰减项；X₀^f = X₀^g a.s.；
5. **经验 W1 引理两分支**：紧支撑（A5 改紧支撑，δ>0 传播保持 sup|c| ≤
   max(C₀, η/δ)）→ N^{-1/2} 干净；2+ε 矩分支如实给 N^{-ε/(2(2+ε))}（不是
   N^{-1/2}）；Fournier–Guillin d=1,p=2 为备选引用（references 已加 FG15）；
6. **误差记号分离**：ε_{0,N}^{coup}（初始耦合，精确初始化 = 0）与
   E‖π_N−π‖₁ 独立；
7. **ε_history 定稿**：ηΛ²T²e^{LT}/(2N)，Λ = ‖a‖_∞‖W‖_∞Ψ_max²，缺失事件
   直接代价 ≤ η（不乘 1+L_Q），A(0)=0 显式，i≠j 明示；
8. **文献修正**：AG25 随机图误差 O(√(log N/N))（v0.3 误写 O(log N/N)）；
   "非马尔可夫"表述改为"增广状态维数随 N² 增长的马尔可夫系统"。

## G4 补记（2026-08-12 四审核验）：v0.4 → v0.5 修订

**用户核验结论（v0.4）**：方向对了但未通过——**T1/T2 卡在 D 的定义与耦合
实现**：V1 未通过（不同总质量有限测度无普通耦合 Cpl=∅；1_{r≠r'} 不连续，
δ₀ vs δ_ε 时 D=1+ε 不趋于 0，A3 无法控制）；V3 未通过（每对时钟 × 目标率
接受使目标总率高 N 倍；Dcore 需平均目标界含 1/N 自环项；c' 独立抽样
E|Ỹ_i−c'|=O(1) 不可用，须最优输运配对；经验测度须显式三角分解）；
**紧支撑方案不可用**（纯跳过程有限时间跳数 Poisson 分布控制，无确定性
上界，支撑立即丢失；η/δ 不适用）；**2+ε 矩足够**（v0.4 退化分支漏全局
N^{-1/2} 因子：E W1 ≤ N^{-1/2}∫√(F(1−F))dx，而尾积分
∫₁^∞√(m_q/x^q)dx = √m_q/(q/2−1) < ∞，q>2）；FG 参数为 d=1, p=1, q>2；
V7（T3 排除项）通过。

**v0.5 修订**（WP3/WP4/proofs.tex 同步，编译通过 6 页）：
1. **墓地度量 D_Λ**：ν̄ᵢ = νᵢ + (Λ−νᵢ(ℝ))δ_∂ 等质量化（耦合恒存在），
   E_∂ = [−1,1]∪{∂}，ρ(r,r')=|r−r'|，ρ(r,∂)=1；对偶 h(∂)=0；
   D_Λ(ν_f(x),ν_g(y)) ≤ L_x|x−y| + L_f d_T(f,g)（Σπ=1 ⇒ 无 K 因子）；
2. **每目标一个 Λ-支配时钟**（缩放错误修复）；副本独立性保持；
3. **平均目标界**：E[(1/N)Σ_j D_Λ] ≤ C(D_t + N^{-1/2} + E‖π_N−π‖₁ + 1/N)，
   1/N 来自 i≠j 自项；标记差沿最优输运计划配对（平均距离 = 经验 W1）；
4. **经验 W1 引理（q>2 初等证明）**：E W1 ≤ C_{q,m_q} N^{-1/2}，
   C_{q,m_q} = 1 + 2√m_q/(q/2−1)；FG15 (d=1,p=1,q>2) 标准引用；
5. **A5 改统一 q 阶矩**，紧支撑删除；A4 恢复 δ ≥ 0；
6. **ε_history 不变**：ηΛ²T²e^{LT}/(2N)（排除 = 送墓地，代价 ≤ 被删率质量）。

## G4 补记（2026-08-12 五审核验）：v0.5 → v0.6 修订

**用户核验结论（v0.5）**：已跨过结构性难关，无新反例；V1、V3 为"结论成立、
正文尚需精算"，经验 W1 引理（V5b）**正式通过**。待修：Λ = A·Ψ_max² 在
Ψ_max<1 时 M_f(ℝ) ≤ AP 可 > AP² 致 D_Λ 无定义；生成元差应逐点估计（不积分
f+g）；d_t 不等式缺 η；c_i' 应写为最优输运计划的条件核；类型内样本量 N_ℓ
随机（E[(N_ℓ/N)N_ℓ^{-1/2}] = (1/N)E√N_ℓ ≤ √(π_ℓ/N)，和 ≤ √(K/N)）；
T1 在 A1–A4 下直接用 q 阶矩（应先证一阶矩传播）；类型-资本分解先用 p^N 权重
再比较 π；经验 W1 常数用传播后 M_{q,T}；"left tail"改"treated analogously"。

**v0.6 修订**（WP3/WP4/proofs.tex 同步，编译通过 7 页，仅剩 <7pt 段落级
overfull）：
1. **Λ = AP²**（P = max{1, Ψ_max}）：M_f 与 ν_f 都在 D_Λ 定义域；
2. **关键估计定稿**：D_Λ(ν_k[f](x), ν_k[g](y)) ≤ B(|x−y| + d_T)，**B = AP(L_ψ
   + PL_Q)**，无 K 因子（常数逐项对账：APL_ψ|x−y| + AP²L_Q|x−y| + AP(L_ψ+PL_Q)d_T）；
3. 生成元差**逐点**（固定状态 c）：|(L_f−L_g)φ(c)| ≤ ηL_φ·D_Λ；墓地耦合即
   接受/拒绝（无独立 thinning）；d_t ≤ ηB∫(d_s + d_T)ds（补 η）；
4. **平均目标界最终形式**：≤ 2B·D_t + Λ·E‖π_N−π‖₁ + B·C_{q,T}·√(K/N)
   + Λ/N（自项精确 Λ/N；随机 N_ℓ 加权 √(K/N)；最优输运计划分解为条件核）；
5. 经验 W1 引理通过：C_{q,m} = 1 + 2√m/(q/2−1)（应用 M_{q,T}）；
6. T1 分层：A1–A4 一阶矩传播，A5 下 q 阶矩传播为推论；
7. 类型-资本分解：先 Σp_k^N W1(μ̃_k, f_k) 再比较 p^N 与 π；联合度量
   d((k,c),(ℓ,c')) = 1_{k≠ℓ} + |c−c'| 显式。

## G4 正式通过 ✅（2026-08-12 六审核验，v0.7 理论完成）

**用户最终核验结论**：定理结构与收敛阶成立；V2、V4、V5 通过；Dcore 最终
公式成立；三处局部修正（Dcore 权重统一 p^N、V6 系数 ‖a‖P²‖W_N−W‖、WP3
引理 p 权重 + √((K−1)/N)）后**理论部分正式完成，G4 关闭**。

**v0.7 修正清单**：
1. Dcore 类型权重统一为经验权重 p_ℓ^N：D_Λ(Σp^N R, Σπ R̄) ≤ Σp_ℓ^N D_Λ(R_{j,ℓ},R̄_ℓ)
   + AP‖p^N−π‖₁，乘 ψ₂ ≤ P 后 Λ‖p^N−π‖₁；与 E[p_ℓ^N N_ℓ^{-1/2}] ≤ √(π_ℓ/N)
   逐式衔接；最终 2B、Λ、√(K/N)、Λ/N 不变；
2. V2 常数显式：dm_q^k/dt ≤ −qδm_q^k + ΛC_{q,η}(1+m_q^k) ⇒ M_{q,T} ≤
   (m_q + ΛC_{q,η}T)e^{ΛC_{q,η}T}；收缩时间只依赖 ηB ⇒ 延拓常数统一；
3. V4 补偿子公式：D_t ≤ D_0 − δ∫D_s ds + η∫(1/N)ΣE D_Λ ds（两次跳跃间
   Δ_j' = −δΔ_j ⇒ |Δ_j(t)| = e^{−δ(t−s)}|Δ_j(s)|，衰减不放大误差）；
4. V5 干净常数：E‖p^N−π‖₁ ≤ (1/√N)Σ_k√(π_k(1−π_k)) ≤ √((K−1)/N)；
5. **V6 系数修正**：v0.6 误写 A·Ψ_max² = ‖a‖‖W‖·Ψ_max²（多乘 ‖W‖_∞ 且
   Ψ_max 未换 P）；正确 D_Λ(ν_j^{W_N},ν_j^W) ≤ **‖a‖_∞·P²·‖W_N−W‖_∞**；
6. WP3 §2.4 引理同步：W1(μ,f) ≤ Σ_k p_k W1(μ_k,f_k) + C_T‖p−π‖₁；
   √(2K/(πN)) 换 √((K−1)/N)。

**[V] 项最终状态**：V1/V2/V3/V4/V5/V5b/V6/V7 **全部销项**。

**理论部分交付物（定稿）**：`paper/appendix/proofs.tex` v0.7（编译通过 7 页，
仅 6.4pt/3.9pt 轻微超宽）、`notes/WP3_kinetic_derivation.md` v0.7、
`notes/WP4_proof_sketch.md` v0.7。主定理：
sup E W1(μ_t^N,f_t) ≤ C_T{N^{-1/2} + ‖W_N−W‖_∞ + N^{-1}}（iid 类型），
一般形式含 ε_{0,N}^{coup} 与 E‖π_N−π‖₁；ε_history ≤ ηΛ²T²e^{LT}/(2N)。

**下一步（按约定顺序）**：RfA 实证修复——(1) 窗口构造（候选人-轮次风险集）；
(2) 窗口前资本污染（第一票污染）；(3) 回归补齐 recency 与发送活跃控制；
(4) 重跑并核验审计数字（3,935 窗口、87.8% 零资本、AUC 0.534、481 非零
窗口 AUC 0.684）；(5) 更新 notes 与 main.tex 主张。

**下一步**：阶段 5（WP5）——估计与留出验证（实证主战场）。

---

## G5（WP5 完成，2026-08-11）

**判定：未通过 ❌（按计划 WP5 门槛）** —— 宏观模型不能优于简单基线，不能以"预测
有效"作为贡献；**诊断完成**（核估计、资本定义、模型闭合三项排查）。

**留出结果**（主切分 70% 时间跨度，测试 2,601 事件，`experiments/holdout.json`）：

| 模型 | 部分似然 | top-10 recall | MRR |
|---|---|---|---|
| 齐次/仅活跃 | −3.425 | — | — |
| 优先连接 α=1 | −3.241 | 0.180 | 0.076 |
| REM（协变量） | −3.352 | 0.298 | 0.194 |
| **动力学模型** | **−3.631** | **0.064** | **0.038** |

宏观：预测人均率 0.0078/天（平坦）vs 实现 0.0017→0.0003（6 倍下降），IAE 16.3；
事件加权资本分布 W1=1.60, JSD=0.24。

**诊断结论**（三项排查）：
1. **模型闭合失败（主因）**：数据中资本渠道近乎平坦（REM：count_cap +0.01、
   net_cap −0.006），主导预测的是 recency（−1.14）、indegree（+0.20）、发送活跃
   （−0.66）——即 WP0 §10 预言的"残余 recency 效应超出资本"。平滑资本函数 ψ2
   无法捕捉注意力/近期性渠道；动力学模型把质量推向高资本节点，而实现目标不是它们。
2. **区间漂移（次因）**：70% 时间跨度切分把测试放在平台衰退尾部；训练期标定的
   静态活跃度外推失败（预测平坦 vs 实现 6 倍下降）。事件分位数切分下模型仍只
   胜过均匀基线（−3.322 vs PA −3.159、REM −2.350）——失败是结构性的，非切分选择。
3. **核估计/资本定义**：排除。核 Ŵ 在 REM 中有正贡献（logW +0.50）；资本定义
   （net/count/quality）在 WP1 已分离。

**计划 §12 出口判定**：第 4 点（留出预测）失败、第 3 点（有限时间理论）很强
（G4 通过，数值交叉验证 N^{-1/2} 阶吻合）→ **转数学期刊路线**（JCN 理论+诊断
定位，或 EJAM 纯理论加强）。实证材料保留为诚实的机制诊断：G1 探针（资本反馈
可识别但符号依赖）、REM 结构（recency 主导的注意力机制）、区间漂移发现。

## G5 补记（2026-08-11）：Wiki-RfA 跨场景验证（第三数据集）

按用户规则执行（单独估计、不合并、构念可比性优先）：`scripts/08_wiki_rfa.py`
→ `experiments/wiki_rfa.json`。审计与用户数字完全一致。发现：
1. 窗口内目标选择的资本结构**跨场景复现**（count +0.005***、net 条件 −0.049***，
   与 OTC 的 +0.033/−0.064 结构一致）；
2. **RfA 中资本有效**：符号预测改善 8.6%、选举结果预窗口资本 AUC 0.796
   （干净检验）——制度性声誉场景资本机制起作用；
3. 与 OTC 失败构成**适用边界对照**：窗口化方向性声誉判断（RfA）资本有效，
   开放注意力驱动选择（OTC）资本闭合失效。到达率形状两场景均失败（模型无
   窗口/衰退结构）。
叙述位置：论文 §6/讨论——扩大理论适用边界，不是 OTC 失败的事后补救。

## G5 补记 2（2026-08-12）：RfA 管线修复与主张撤回 ⚠️

**投稿前审计**发现 08_wiki_rfa.py 两个代码缺陷：(1) `window_target_choice`
以候选人为键保留**最后一个窗口**（多轮申请被压成一轮，1,521 层不对应完整
候选人-轮次风险集）；(2) `election_validation` 的 cap_at_start 以候选人为键
（多轮压一轮）且**先加本轮第一票再记录"窗口前资本"**——AUC 0.796 受第一票
污染。另：RfA 回归缺 OTC 的 recency/发送活跃控制，"结构复现"只是探索性相似。

**修复后重跑**（`experiments/wiki_rfa.json`，2026-08-12）：

| 指标 | 修复后 | 旧主张 |
|---|---|---|
| 有效窗口（outcome ±1） | **3,935**（与审计复核一致） | — |
| 窗口前资本为零占比 | **87.8%**（3,478/3,935） | — |
| 真实窗口前资本 AUC | **0.534** | ~~0.796~~ **撤回**（第一票污染） |
| 非零窗口前资本（重复申请） | 481 窗口，AUC **0.684**（事后子样本，非确认性） | — |
| 期末资本 AUC | 0.878 同样受污染，仅作对照 | — |
| 符号预测（未动） | logloss 0.552 vs 基线 0.604（+8.6%）；三分类 0.719 | 保留 |
| **对齐 OTC 特征后目标选择** | net +0.032**、count −0.004***、**recency −2.54*** 主导（p<0.001），log_indegree −0.19、age +0.0015、ever_sent +0.38；1,555 层，IRWLS 收敛 | ~~结构复现~~ **撤回** |
| 到达率形状 | shape_corr **−0.62**（负相关，失败） | 失败 |

**科学结论修正（诚实报告）**：
1. **"目标选择资本结构跨场景复现"撤回**——补齐 recency/发送活跃控制并对齐
   风险集后，RfA 与 OTC 结构不一致（OTC count +0.033/net −0.064 vs RfA
   count −0.004/net +0.032，符号相反）；**recency 在两场景均主导**
   （RfA −2.54、OTC −1.14）——"选择由近期注意力驱动"的发现跨场景成立，
   "资本在选择中的结构"不成立；
2. **"RfA 中资本预测选举结果（AUC 0.80）"撤回**——干净检验（窗口前资本）
   为 AUC 0.534（接近无信息），87.8% 窗口前资本为 0（首次申请占绝大多数，
   无历史资本可预测）；重复申请子样本 0.684 是事后子样本，不能作确认性
   结果；期末资本 0.878 含本轮票，无预测意义；
3. **仍然成立的发现**：符号预测改善 8.6%（资本 → 未来票方向，该检验未受
   窗口 bug 影响）；到达率形状两场景均失败；**资本闭合的边界叙述收紧**：
   模型（资本强度）无法解释任何场景的目标选择（recency 主导），RfA 的
   选举结果也不由窗口前资本显著预测——"资本闭合失效"从 OTC 特例升级为
   两场景共同边界（符号方向除外）；
4. 叙述调整：删除摘要/结果/讨论中的 AUC 0.796/0.80 与"结构复现"表述
   （WP6 §3.6 与 main.tex 同步，任务 #19）。

**窗口重建可靠性**（不受影响）：3,950 窗口、gap 60 天、中位 6.4 天——
审计复核与用户数字一致（188,904/11,128/7,337/8,044）。

**用户决策（2026-08-11）：A1/JCN 理论优先 ✅**（用户明确细化）：
- 论文定位：主要贡献 = 有向带标记信任事件的微观—宏观推导；理论贡献 = 有限时间
  传播混沌 + 三项分解；数值贡献 = N^{-1/2} 阶验证与求解效率；经验贡献 = 一次
  **模型充分性压力测试**（发现 Bitcoin OTC 由近期注意力与平台衰退主导，资本闭合
  在该场景失效），禁用"模型通过真实数据验证/成功解释"表述，统一措辞为
  "The empirical application serves as a model-adequacy stress test and
  identifies a clear boundary of capital-only kinetic closure"；
- G4 为**条件通过**：逐项完成并核验 proofs.tex 的 [V] 项后再视为理论完成；
- G5 改写为简短的 empirical stress test 小节，不作为主要预测结果；
- Hawkes-recency 扩展列为后续研究（不在本篇实现）；
- 理论主文完成后做一次 EJAM/JCN 路由复核；
- 暂定题目："From Directed Trust Events to Reputation-Capital Kinetics:
  A Finite-Time Limit and an Empirical Stress Test"。

**下一步**：阶段 6（WP6）——逐项完成证明 [V]、论文写作（理论优先 9 段结构、
压力测试叙述）+ 复现包 + 路由复核。

## G5 补记 3（2026-08-12）：OTC/Alpha 估计与留出状态审计（最终修正版）

投稿前复现审计发现并修复：资本响应轮廓未真正使用 $\alpha$；块曝光把组规模
重复相乘；估计的全局率尺度未传入动理学求解器；活动时间未截止到训练期末；
留出期风险集/indegree/recency/发送历史未从训练期状态初始化；评分幅度被重复除以
10；经验幅度分箱索引错误；宏观初值未保留类型条件资本分布。修复后所有参数仅用
训练期估计，并重跑 `06_holdout.py` 与 `07_robustness.py`。

最终 OTC：动力学/均匀/PA/REM 部分似然分别为 **−3.442/−3.434/−2.632/−1.104**；
全风险集 top-10 为 **0.148/—/0.085/0.324**。因此动力学在部分似然上仅与均匀
相当，但其 top-10 高于 PA；REM 的 recency 通道仍显著领先。宏观预测均值
0.00466/人日 vs 实现 0.000775（6.0 倍），预测轨迹近乎平坦、实现率下降约 6.2
倍；事件加权资本分布 $W_1=1.325$。Alpha：动力学 −3.343 vs 均匀 −3.434，
top-10 0.127，但宏观到达率形状继续失败。科学定位由“差于全部基线”校准为：
**块/资本结构提供有限排序信息，但缺少 recency 与生命周期通道，无法形成充分的
微观选择或宏观到达率闭合。**

## G6（2026-08-12）：WP4 证明门正式关闭，理论封版（v0.11）

**用户判定：The proof audit is closed. The two-dimensional (C,H)
propagation-of-chaos result retains the N^{-1/2} particle-coupling rate.**

- v0.9 → v0.10 → v0.11 三轮审核闭环：v0.9（1 Critical + 3 Major，标记率
  引理及其进入 Dcore 的方式）→ v0.10（三步分解、辅助标记/条件 Jensen、
  Λ=AP₁max{1,P_T}、pre/post-target 常数）→ v0.11（iii-a 直接有限测度
  耦合 D_Λ(wQ,w'Q')≤|w−w'|+min(w,w')W₁(Q,Q')，引理常数 C=3AP₁）；
  用户逐项核验全部通过，无遗留项；
- **定稿内容**：Dcore 三步分解 R^N(X_i^N,X_j^N)→R̃^N(Ẋ_i,X_j^N)→
  R̃^N(Ẋ_i,Ẋ_j)→R^∞(Ẋ_j)（iii-c 条件于 (Ẋ_j,types) 用引理）；标记率
  引理不归一化质量/CDF 波动、无 1/μ；D_t 系数对账 ≤2B；
- proofs.tex v0.11 已清除全部 [V] 标记与审计过程语言（含 TV 反例），
  0 error/0 undefined/0 overfull；WP4/WP6 notes 全部旧"待核验"状态
  已标注销项；
- **用户决策（2026-08-12）**：理论封版，**不再改动证明结构与常数**；
  JCN 路由判定复核成立（理论基础达到"可以认真投 JCN"程度）；
- **下一阶段（整稿级审稿，6 项优先级）**：① 贡献定位（相对复杂网络/
  信誉网络/均场与传播混沌文献的新增）；② 理论—实证闭环（"资本闭合不足→
  残差诊断→引入 H→跨平台复制→二维理论扩展"唯一主线）；③ 控制贡献数量
  （模型/定理/预测实验/平台衰退/方法诊断不分设为同等级贡献）；④ JCN
  适配（强化网络机制与复杂系统意义，弱化纯预测竞赛感）；⑤ 投稿前审计
  （引用、图表、占位符、数据与代码声明、摘要与结论主张强度）；⑥ 英文
  压缩与投稿包装（最后做）。首选任务：**模拟 JCN 审稿人整稿审查**
  （编辑为何送审、审稿人最可能攻击哪里、距可接收状态差哪些实质内容）。
